initialPlaylist:  Cubotron #Color Cube #All Videos
initialPlayState: loop_scene #playing

# CUBOTRON
# TESSERACT
# TESSERACT_WALL
# DRACO
# SCARED
stageType: TESSERACT #SCARED

# this is temporary.  eventually we'll just have a list of controllers or whatever makes sense, e.g.
# controllers:
#   - type: rabbit
#     ip: 192.168.0.100
#   - type: rabbit
#     ip: 192.168.0.101
#   ...
numRabbits: 0
numTeensies: 5

# Serial port the rotary encoder's Arduino Mega is on. Empty/omitted disables the encoder.
rotaryEncoderPort: /dev/cu.usbmodem101
